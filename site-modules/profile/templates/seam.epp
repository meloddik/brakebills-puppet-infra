network:
  version: 2
  renderer: networkd
  ethernets:
    <%= lookup('seam-interface', String, 'first') %>:
      addresses:
        - <%= lookup('seam-cidr', String, 'first') %>
      routes:
        - to: default
          via: 10.1.30.1
      mtu: 1500
      dhcp4: false
      dhcp6: false
